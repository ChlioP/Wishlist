export function FoundationPage() {
  return (
    <main className="grid min-h-screen place-items-center bg-cream px-6 text-ink">
      <section className="w-full max-w-xl rounded-3xl border border-soft bg-white p-10 text-center shadow-soft">
        <div className="mx-auto mb-5 h-3 w-3 rounded-full bg-pink" />
        <h1 className="font-display text-4xl">WishList Hub</h1>
        <p className="mt-4 text-sm leading-6 text-muted">
          The React, TypeScript, Tailwind, and routing foundation is ready.
        </p>
      </section>
    </main>
  );
}
